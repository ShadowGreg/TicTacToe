using System.Data;
using TicTacToe;


int size = 3;
var playField = new PlayField(size);
//Motion FirstPlayer = new Motion(playField);
//Motion SecondPlayer = new Motion(playField);
// int row = 1;
// int column = 1;

//FirstPlayer.SetMotion(row, column, PlayerIcon.O);
//SecondPlayer = FirstPlayer.GetComputerMotion();